@model Customer
<div class="well">
    <h3>
        <strong>@Model.FirstName @Model.LastName</strong>
        <span class="pull-right labele label-primary">
            @Model.BithDate.ToString()
        </span>
    </h3>
    <form id=" @Model.CustomerID" asp-controller="Room" method="post">
        <input type="hidden" asp-for="CustomerID" />
        <input type="hidden" name="returnUrl" value="@ViewContext.HttpContext.Request.PathAndQuery()" />
        <span class="lead">
            @Model.PhoneNumber
            </br>
            @Model.Email
            <button type="submit" class="btn btn-success btn-sm pull-right" asp-action="ConfirmDeleteCustomer">
                Удалить
            </button>
            <button type="submit" class="btn btn-success btn-sm pull-right" asp-action="EditCustomer">
                Редактировать
            </button>
        </span>
    </form>
</div>